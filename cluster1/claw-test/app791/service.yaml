apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app791-svc
  namespace: clawtest
  labels:
    app: perf-test2-app791
spec:
  selector:
    app: perf-test2-app791
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
