apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app54-svc
  namespace: clawtest
  labels:
    app: perf-test2-app54
spec:
  selector:
    app: perf-test2-app54
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
