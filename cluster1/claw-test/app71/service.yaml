apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app71-svc
  namespace: clawtest
  labels:
    app: perf-test2-app71
spec:
  selector:
    app: perf-test2-app71
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
