apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app178-svc
  namespace: clawtest
  labels:
    app: perf-test2-app178
spec:
  selector:
    app: perf-test2-app178
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
