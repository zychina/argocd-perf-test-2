apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app980-svc
  namespace: clawtest
  labels:
    app: perf-test2-app980
spec:
  selector:
    app: perf-test2-app980
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
