apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app3-svc
  namespace: clawtest
  labels:
    app: perf-test2-app3
spec:
  selector:
    app: perf-test2-app3
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
