apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app589-svc
  namespace: clawtest
  labels:
    app: perf-test2-app589
spec:
  selector:
    app: perf-test2-app589
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
