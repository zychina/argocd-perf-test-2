apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app766-svc
  namespace: clawtest
  labels:
    app: perf-test2-app766
spec:
  selector:
    app: perf-test2-app766
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
