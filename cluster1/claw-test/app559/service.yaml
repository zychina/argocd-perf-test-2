apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app559-svc
  namespace: clawtest
  labels:
    app: perf-test2-app559
spec:
  selector:
    app: perf-test2-app559
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
