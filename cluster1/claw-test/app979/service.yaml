apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app979-svc
  namespace: clawtest
  labels:
    app: perf-test2-app979
spec:
  selector:
    app: perf-test2-app979
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
