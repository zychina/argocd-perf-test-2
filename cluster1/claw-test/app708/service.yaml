apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app708-svc
  namespace: clawtest
  labels:
    app: perf-test2-app708
spec:
  selector:
    app: perf-test2-app708
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
