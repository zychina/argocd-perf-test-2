apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app265-svc
  namespace: clawtest
  labels:
    app: perf-test2-app265
spec:
  selector:
    app: perf-test2-app265
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
