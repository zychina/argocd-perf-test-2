apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app161-svc
  namespace: clawtest
  labels:
    app: perf-test2-app161
spec:
  selector:
    app: perf-test2-app161
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
