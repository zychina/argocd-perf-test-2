apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app203-svc
  namespace: clawtest
  labels:
    app: perf-test2-app203
spec:
  selector:
    app: perf-test2-app203
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
