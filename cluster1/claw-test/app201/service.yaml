apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app201-svc
  namespace: clawtest
  labels:
    app: perf-test2-app201
spec:
  selector:
    app: perf-test2-app201
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
