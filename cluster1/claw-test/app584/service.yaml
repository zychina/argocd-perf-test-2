apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app584-svc
  namespace: clawtest
  labels:
    app: perf-test2-app584
spec:
  selector:
    app: perf-test2-app584
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
