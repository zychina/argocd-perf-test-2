apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app174-svc
  namespace: clawtest
  labels:
    app: perf-test2-app174
spec:
  selector:
    app: perf-test2-app174
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
