apiVersion: v1
kind: Service
metadata:
  name: perf-test2-app1-svc
  namespace: clawtest
  labels:
    app: perf-test2-app1
spec:
  selector:
    app: perf-test2-app1
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
